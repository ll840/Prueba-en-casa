

int numero, suma = 0;  // numero ya no tiene que se inicializado

printf("Introduce un número: ");
scanf("%d", &numero);   // lee número de siguiente iteración


while (numero > 0)

{  // El bucle se ejecutará mientras el número sea diferente de un negativo
    suma = suma + numero
    printf("Introduce un número: ");
    scanf("%d", &numero);   // lee número de siguiente iteración
    suma = suma + numero;
}

suma= suma - numero
printf("La suma de los números ingresados es: %d\n", suma);

//segundo problema

while (cociente != 0) {
    resto = cociente % 2;
    cociente = cociente / 2;
    printf (//Se pone a ver qué es lo que se está mostrando)
    binario = binario + resto * factor;
    factor = factor * 10;
}

//Podemos poner print en mitad del print para ver si se está cumpliendo

